from setuptools import setup

setup(name='dfsummary',
      version='0.4',
      description='A dataframe EDA helper package',
      packages=['dfsummary'],
      author='Matthew Perkins',
      zip_safe=False)
